"""MCP client wrappers — thin adapters over the MCP server tool functions.

Development mode: imports server functions directly (same process).
Production: swap these for HTTP MCP client calls when servers run standalone.
"""
